import log from '../utils/logger';

export default ( ) => {
	log.info( '=== Create WP Site ===' );
	log.info( 'Create WP Site is a site creation wizard for WordPress using 10up/wp-docker' );
	log.info( 'Run create-wp-site --help for more info' );
};
